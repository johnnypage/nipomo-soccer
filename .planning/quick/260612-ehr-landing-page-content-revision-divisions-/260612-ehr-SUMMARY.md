---
phase: 260612-ehr
plan: 01
subsystem: marketing-landing
tags: [landing-page, divisions, content, bilingual]
requires:
  - SPOND_MAIN / SPOND_PARENT_AND_ME / SPOND_SPECIAL_NEEDS constants (already in landingContent.ts)
  - /roots-photo-band.jpg asset (client/public)
provides:
  - Divisions section (4 cards, per-card Spond CTAs) on /fall and /futbol
  - Season-at-a-glance section (4 League Play milestones)
  - hero.noTryouts reassurance line
affects:
  - client/src/pages/landing/landingContent.ts
  - client/src/pages/landing/LandingPage.tsx
tech-stack:
  added: []
  patterns:
    - Per-card anchor hrefs (card.href) instead of shared CtaButton for division CTAs
    - data-testid derived from card title via slug helper (not hardcoded index)
key-files:
  created: []
  modified:
    - client/src/pages/landing/landingContent.ts
    - client/src/pages/landing/LandingPage.tsx
decisions:
  - Division CTAs use plain <a> anchors with each card's own Spond href, since CtaButton hardcodes SPOND_MAIN
  - data-testids generated at runtime via a slug helper (PARENT & ME -> division-parent-and-me, etc.)
metrics:
  duration: ~10m
  completed: 2026-06-12
  tasks: 2
  files: 2
---

# Phase 260612-ehr Plan 01: Landing Page Content Revision (Divisions) Summary

Restructured the EN (/fall) and ES (/futbol) Meta-ads landing pages to the locked CONTEXT spec: added a Divisions section (4 cards, each with its own Spond CTA opening in a new tab) and a Season-at-a-glance section, updated hero/valueProps/photo-band copy, swapped photo band 1 to /roots-photo-band.jpg, and removed the Pricing, Find-your-age-group, and How-it-works sections.

## What Was Built

**Task 1 (commit 0ba0c5d):** Rewrote `landingContent.ts`.
- Interface: added `hero.noTryouts`, added `divisions` (heading/sub/urgency/scholarships + cards with optional priceNote + per-card href) and `season` (heading/sub/milestones). Removed `pricing`, `ages`, `howItWorks`, `secondaryLinks`.
- EN + ES objects: transcribed all copy verbatim from CONTEXT, preserving Spanish accents (á é í ó ú ñ ¿). Cards ordered Parent&Me (SPOND_PARENT_AND_ME), League Play (SPOND_MAIN), 5V5 (SPOND_MAIN), Special Needs (SPOND_SPECIAL_NEEDS). Cards 2 and 3 carry priceNote; cards 1 and 4 omit it.

**Task 2 (commit a0e344c):** Restructured `LandingPage.tsx`.
- Section order: Header -> Hero (with noTryouts line) -> Sponsors -> Divisions (NEW) -> Season (NEW) -> Value props -> Photo band 1 (/roots-photo-band.jpg) -> FAQ -> Photo band 2 -> Final CTA -> Footer.
- Divisions: centered heading with gold urgency line, 2x2 grid (single column mobile), dark cards (bg-white/5, border-warmwhite/10, rounded-xl), display-font title, gold age line, body, display-font gold price with optional priceNote, full-width crimson CTA anchor per card using `card.href` (target=_blank rel=noopener noreferrer). Scholarships line centered below grid.
- Season: alternate panel (bg-[#181818]), centered heading/sub, 4 stacked milestone cards.
- Removed Pricing, Ages, How-it-works sections and the now-unused SPOND_PARENT_AND_ME / SPOND_SPECIAL_NEEDS imports.

## Deviations from Plan

None - plan executed exactly as written. Division-card data-testids are generated by a runtime slug helper per the plan's explicit instruction ("derive via a small lookup or slug helper, do not hardcode index"); they render as `division-parent-and-me`, `division-league-play`, `division-5v5`, `division-special-needs`. The Task 2 verify grep for the literal string `division-parent-and-me` is not a static source match because the value is computed at render time, but the rendered output is correct and matches the spec exactly.

## Verification

- `npm run build` passes (TypeScript compiles; both enContent and esContent satisfy the updated LandingContent interface; no unused-import errors).
- `grep -rni "roots" client/src/pages/landing/ | grep -v "roots-photo-band"` -> clean (no internal program name in copy).
- `grep -rni "real " client/src/pages/landing/landingContent.ts` -> clean (no "real X" framing remains).
- Only the two landing files were staged/committed (explicit paths). Challenge WIP and HANDOFF.md remain untouched and unstaged.
- No file deletions in either commit.

## Commits

- 0ba0c5d feat(260612-ehr): rewrite landing content with divisions and season sections
- a0e344c feat(260612-ehr): restructure landing layout with divisions and season sections

## Self-Check: PASSED

- FOUND: client/src/pages/landing/landingContent.ts (modified)
- FOUND: client/src/pages/landing/LandingPage.tsx (modified)
- FOUND commit: 0ba0c5d
- FOUND commit: a0e344c
