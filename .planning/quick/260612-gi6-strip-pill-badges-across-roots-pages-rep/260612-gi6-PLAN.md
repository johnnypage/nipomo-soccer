---
phase: quick-260612-gi6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/pages/roots/HeroSection.tsx
  - client/src/pages/roots/CoachCTASection.tsx
  - client/src/pages/roots/FAQSection.tsx
  - client/src/pages/roots/DivisionSection.tsx
  - client/src/pages/roots/FamilyFeedbackSection.tsx
  - client/src/pages/roots/PathwaySection.tsx
  - client/src/pages/roots/RegistrationSection.tsx
  - client/src/pages/roots/WhatsNewSection.tsx
  - client/src/pages/roots/WhatIsSection.tsx
  - client/src/pages/roots/ParentAndMe.tsx
  - client/src/pages/roots/Recreational.tsx
  - client/src/pages/roots/FiveVFive.tsx
autonomous: false
requirements: [GI6-DESIGN-PILLS]

must_haves:
  truths:
    - "No ROOTS page renders an eyebrow as a pill/chip (no rounded-full border+bg badge above section headings)"
    - "Hero stat row renders as plain inline text, not a row of bordered chips"
    - "Eyebrow labels still read as small uppercase tracked text in gold or crimson above their headings"
    - "Genuine UI circles (checkmark circles, timeline dots, avatars, icon circles) are untouched"
    - "No copy text content changed; only visual chrome removed"
  artifacts:
    - path: "client/src/pages/roots/HeroSection.tsx"
      provides: "Hero stats as plain inline text row, no chips"
      contains: "Ages 2 to high school"
    - path: "client/src/pages/roots/PathwaySection.tsx"
      provides: "Plain eyebrow + plain card tag text, no pill chrome"
  key_links:
    - from: "all roots eyebrow spans"
      to: "section heading below them"
      via: "plain uppercase tracked text, no rounded-full"
      pattern: "tracking-wider uppercase"
---

<objective>
Strip pill/chip/badge chrome from every ROOTS page and section component, keeping the label
text but rendering it as plain display-styled eyebrow text matching the approved landing pages
(client/src/pages/landing/). Johnny's design rule: no hero pills, no stat pill rows, no pill
eyebrow badges.

Purpose: Bring the warm-audience ROOTS pages in line with the approved no-pill design language
already used on the Meta-ads landing pages.
Output: 12 updated ROOTS component files with all pill eyebrow badges, hero stat chips, and card
tag chips converted to plain text. Genuine UI circles preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/HANDOFF-main-site-refresh.md

@client/src/pages/landing/LandingPage.tsx

<reference_patterns>
The landing page is the approved no-pill model. Note how it renders eyebrows/labels as PLAIN text:

Eyebrow / label style (LandingPage.tsx, e.g. season milestone dates and quote attribution):
```tsx
<p className="text-gold text-[13px] font-semibold tracking-[0.14em] uppercase">
  {label}
</p>
```
The landing page never wraps a label in `inline-block px-3 py-1 rounded-full border bg-.../10`.
Eyebrows are just small, uppercase, tracked, color-tinted text. That is the target style.
</reference_patterns>

<pill_inventory>
THREE distinct pill patterns exist across ROOTS. Handle each as described.

PATTERN A -- Eyebrow pill (the dominant pattern, ~22 instances). Strip chrome, keep text.
Exact class string to replace:
  `inline-block px-3 py-1 text-xs font-semibold tracking-wider uppercase rounded-full border border-gold/50 bg-gold/10 text-gold`
  (and the crimson variants: `border border-crimson/30 bg-crimson/10 text-crimson`,
   `border border-crimson/40 bg-crimson/10 text-crimson`)
Replace with PLAIN eyebrow text, preserving the gold/crimson color:
  gold variant   -> `text-xs font-semibold tracking-wider uppercase text-gold`
  crimson variant -> `text-xs font-semibold tracking-wider uppercase text-crimson`
Drop: `inline-block px-3 py-1 rounded-full border border-* bg-*/10`. Keep: text size, weight,
tracking, uppercase, and the color token. Keep any trailing margin utility already on the span
(e.g. `mb-5` on CoachCTASection.tsx:21) so spacing rhythm to the heading is preserved.

Locations (file:line of the opening span):
  CoachCTASection.tsx:21 (gold, has mb-5)
  FamilyFeedbackSection.tsx:98 (gold)
  WhatsNewSection.tsx:101 (crimson)
  WhatIsSection.tsx:7 (crimson)
  FAQSection.tsx:140 (crimson)
  RegistrationSection.tsx:5 (crimson)
  PathwaySection.tsx:41 (crimson)  -- the "The Pathway" eyebrow
  ParentAndMe.tsx:55 (crimson), :79 (gold), :109 (crimson), :156 (gold)
  DivisionSection.tsx:62 (gold)
  Recreational.tsx:111 (crimson), :133 (gold), :224 (crimson), :269 (gold), :290 (crimson)
  FiveVFive.tsx:112 (crimson), :133 (gold), :163 (gold), :189 (crimson), :220 (gold), :289 (crimson)

PATTERN B -- Hero stat pills (HeroSection.tsx:17-23). Currently a wrapped row of bordered chips:
  `px-4 py-1.5 bg-warmwhite/[0.06] border border-warmwhite/10 rounded-full text-warmwhite/65 text-sm font-medium`
Convert to a single plain inline text row in the same muted color, stats separated by a thin
divider character. Render as one centered line (wrap allowed), no chips. Target output:
```tsx
<p className="text-warmwhite/55 text-sm font-medium mt-8 mb-10 max-w-[600px]">
  Ages 2 to high school &middot; 12-week season &middot; Up to 20 games &middot; Paid referees &middot; No tryouts
</p>
```
Use the existing five stat strings verbatim. Use a middot (`&middot;`) as the separator (NOT an
em dash, NOT a double hyphen). Keep the `mt-8 mb-10` rhythm so the CTA row below stays positioned.
You may keep the `.map()` with a `<span>` per stat plus a separator span if you prefer, as long as
the result is plain inline text with no border/background/rounded-full.

PATTERN C -- Pathway card tag chip (PathwaySection.tsx:62). Per-card status tag:
  `inline-block px-2.5 py-0.5 text-xs font-semibold tracking-wide uppercase rounded-full border ${p.tagColor}`
This is a badge/chip, so strip the pill chrome too. Replace with plain tracked uppercase text.
`p.tagColor` is a border+text color class combo; keep ONLY its text-color portion if cleanly
separable, otherwise fall back to a neutral plain style:
  `text-xs font-semibold tracking-wide uppercase ${p.tagColor}` is acceptable ONLY if tagColor
  contains a text-* color. If tagColor is border-only, use a plain `text-slate` style instead.
Read the PROGRAMS array `tagColor` values at the top of PathwaySection.tsx before editing to make
the right call. Drop `inline-block px-2.5 py-0.5 rounded-full border`.

DO NOT TOUCH (genuine UI circles, NOT badges):
  FamilyFeedbackSection.tsx:156 -- `w-5 h-5 rounded-full bg-risegreen/10 ... checkmark circle`
  Any `rounded-full` on buttons, avatars, timeline dots, icon circles.
  Any `rounded-lg` (these are buttons/cards, never in scope).
</pill_inventory>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip eyebrow pills (Pattern A) across all 12 ROOTS files and fix hero stats (Pattern B) + pathway card tag (Pattern C)</name>
  <files>client/src/pages/roots/HeroSection.tsx, client/src/pages/roots/CoachCTASection.tsx, client/src/pages/roots/FAQSection.tsx, client/src/pages/roots/DivisionSection.tsx, client/src/pages/roots/FamilyFeedbackSection.tsx, client/src/pages/roots/PathwaySection.tsx, client/src/pages/roots/RegistrationSection.tsx, client/src/pages/roots/WhatsNewSection.tsx, client/src/pages/roots/WhatIsSection.tsx, client/src/pages/roots/ParentAndMe.tsx, client/src/pages/roots/Recreational.tsx, client/src/pages/roots/FiveVFive.tsx</files>
  <action>
For each Pattern A eyebrow span listed in <pill_inventory>, edit the className to remove
`inline-block px-3 py-1 rounded-full border border-{gold|crimson}/{NN} bg-{gold|crimson}/10`,
keeping `text-xs font-semibold tracking-wider uppercase text-{gold|crimson}` plus any existing
trailing margin utility (e.g. mb-5). Do NOT change the label text inside the span.

For Pattern B in HeroSection.tsx, replace the stat-pill `.map()` block (lines ~17-23) with a single
plain inline text row using `&middot;` separators in `text-warmwhite/55`, preserving `mt-8 mb-10`
spacing and all five stat strings verbatim. No chips, no border, no rounded-full.

For Pattern C in PathwaySection.tsx, first read the PROGRAMS array `tagColor` values, then strip
the chip chrome from the card tag span (line ~62), keeping plain uppercase tracked text per the
guidance in <pill_inventory>. Also handle the "The Pathway" eyebrow at line ~41 as Pattern A.

Touch only chrome. Never alter copy text. No em dashes or double hyphens anywhere (use &middot;
for the hero separator).
  </action>
  <verify>
    <automated>cd /Users/johnnypage/Projects/nipomo-soccer-website && echo "=== remaining rounded-full in roots (should ONLY be genuine UI circles like checkmark/dots) ===" && grep -rn "rounded-full" client/src/pages/roots/ && echo "=== eyebrow/stat pill chrome must be GONE (expect 0) ===" && grep -rcn "rounded-full border border-\(gold\|crimson\)" client/src/pages/roots/ | grep -v ':0$' || echo "PASS: no eyebrow pill chrome remains" && echo "=== hero stat chip must be GONE (expect no match) ===" && grep -n "bg-warmwhite/\[0.06\] border border-warmwhite/10 rounded-full" client/src/pages/roots/HeroSection.tsx && echo "FAIL: hero stat pill still present" || echo "PASS: hero stat pill removed"</automated>
  </verify>
  <done>
grep shows zero `rounded-full border border-gold` and zero `rounded-full border border-crimson`
matches in client/src/pages/roots/. Hero stat pill chip class is gone from HeroSection.tsx. The
only remaining `rounded-full` matches are genuine UI circles (FamilyFeedbackSection.tsx:156
checkmark and any timeline dots/avatars). No copy text changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Typecheck and build verification</name>
  <files>(no edits -- verification only)</files>
  <action>
Run the TypeScript typecheck / build to confirm the edits introduce no JSX or type errors. Use the
project's existing check script (npm run check, or tsc --noEmit, or npm run build -- whichever the
package.json defines). Do not modify any files in this task; if errors surface, fix them in the
ROOTS files from Task 1 only (never touch challenge/* or shared/* files).
  </action>
  <verify>
    <automated>cd /Users/johnnypage/Projects/nipomo-soccer-website && (npm run check 2>&1 || npx tsc --noEmit 2>&1) | tail -30</automated>
  </verify>
  <done>Typecheck/build completes with no new errors attributable to the 12 edited ROOTS files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Stripped all pill/chip/badge chrome from ROOTS pages: eyebrow pills now plain uppercase gold/crimson text, hero stat chips now a plain inline middot-separated text row, pathway card tags de-chipped. Genuine UI circles (checkmarks, dots) untouched. No copy changed.</what-built>
  <how-to-verify>
1. Run the dev server (npm run dev) and open the ROOTS pages: the main ROOTS hub, /five-v-five (FiveVFive), Parent and Me (ParentAndMe), and Recreational.
2. Confirm every eyebrow above a section heading reads as plain small uppercase tracked text (gold or crimson) with NO pill background/border behind it.
3. Confirm the hero stat line ("Ages 2 to high school", "12-week season", etc.) renders as one plain text row separated by middots, not a row of bordered chips.
4. Confirm visual hierarchy still reads cleanly: eyebrow, then heading, with the same spacing rhythm as before.
5. Confirm checkmark circles in the Family Feedback section and any timeline dots are still round and intact.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any eyebrow/stat that still looks like a pill or any spacing that looks off.</resume-signal>
</task>

</tasks>

<verification>
- grep confirms no `rounded-full border border-gold` / `rounded-full border border-crimson` remain in client/src/pages/roots/
- Hero stat pill chip class removed from HeroSection.tsx
- Typecheck/build passes
- Human confirms eyebrows and hero stats render as plain text with intact hierarchy
</verification>

<success_criteria>
- All Pattern A eyebrow pills across 12 files converted to plain text (text + color preserved)
- Hero stat row (Pattern B) is plain inline middot-separated text
- Pathway card tag (Pattern C) de-chipped
- Genuine UI circles untouched
- No copy text changed; no em dashes or double hyphens introduced
- Colors remain within crimson/gold/night/warmwhite palette
</success_criteria>

<git_staging_constraint>
CRITICAL: The working tree has unrelated in-progress challenge edits (client/src/components/challenge/*,
client/src/hooks/*, server/challengeRoutes.ts, shared/*, client/src/pages/challenge/*). When committing,
stage ONLY the 12 ROOTS files in files_modified BY EXPLICIT PATH. NEVER use `git add -A`, `git add .`,
or `git add -u`. Stage each file path individually.
</git_staging_constraint>

<output>
After completion, create `.planning/quick/260612-gi6-strip-pill-badges-across-roots-pages-rep/260612-gi6-SUMMARY.md`
</output>
